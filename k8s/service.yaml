apiVersion: v1
kind: Service

metadata:
  name: springboot-demo-service

spec:
  type: NodePort

  selector:
    app: springboot-demo

  ports:
  - port: 8081
    targetPort: 8081
    nodePort: 30080
